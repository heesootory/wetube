extends base.pug


block content
    h4 영상 제목 수정하기
    form( method = "POST")
        input(name = "title", placeholder="Video Title", value=video.title, required)
        input(type = "submit", value = "Save" )